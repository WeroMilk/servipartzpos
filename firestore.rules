rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Usuarios: cada usuario puede leer/escribir su propio perfil; admin puede leer/escribir todos.
    // Si el documento del usuario no existe aún (primer login), solo permitir acceso a su propio doc.
    match /users/{userId} {
      allow read, write: if request.auth != null && (
        request.auth.uid == userId ||
        (exists(/databases/$(database)/documents/users/$(request.auth.uid)) &&
         get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin')
      );
    }
    
    // Tiendas: cualquier autenticado puede leer (admin ve todas, store_user filtra en app)
    match /stores/{storeId} {
      allow read: if request.auth != null;
      allow create: if request.auth != null;
      allow update, delete: if request.auth != null;
      
      // Productos por tienda
      match /products/{productId} {
        allow read, write: if request.auth != null;
      }
      
      // Ventas por tienda
      match /sales/{saleId} {
        allow read, write: if request.auth != null;
      }

      // Turnos por tienda (apertura/cierre + cortes)
      match /shifts/{shiftId} {
        allow read, write: if request.auth != null;
      }
      
      // Movimientos por tienda
      match /movements/{movementId} {
        allow read, write: if request.auth != null;
      }
    }
  }
}
